@using AdminPanel.Entities
@model DeliveryMethod

@{
    ViewBag.Title = "Add Delivery Method";
}

@using (Html.BeginForm("AddDeliveryMethod", "DeliveryMethod", FormMethod.Post))
{
    <div class="form-horizontal">
        @Html.AntiForgeryToken()

        <div class="well">
            <fieldset>
                <legend>Add Delivery Method</legend>
                <div class="form-group">
                    <div class="col-md-9 col-md-offset-2">
                        @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                    </div>
                    @Html.LabelFor(model => model.Name, new { @class = "requiredA control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(model => model.Name, new { @class = "form-control", @style = "text-align:center" })
                    </div>
                </div>
                <div class="col-md-offset-5 col-md-3">
                    <input type="submit" value="CONFIRM" class="btn btn-success btn-sm" />
                    @Html.ActionLink("CANCEL", "Index", "DeliveryMethod", null, new { @class = "btn btn-danger btn-sm" })
                </div>
            </fieldset>
        </div>
    </div>
}

@section scripts{
    @Scripts.Render("~/bundles/jqueryval")
}

